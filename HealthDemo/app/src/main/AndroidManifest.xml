<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.orkuncoskun.healthdemo">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".StarterApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".HintActivity"></activity>
        <activity android:name=".UpperLowerActivity" />
        <activity android:name=".PushPullActivity" />
        <activity android:name=".SplitActivity" />
        <activity android:name=".FullbodyActivity" />
        <activity android:name=".WorkoutActivity" />
        <activity android:name=".PresportActivity" />
        <activity android:name=".PostsportActivity" />
        <activity android:name=".StretchActivity" />
        <activity android:name=".SnacksActivity" />
        <activity android:name=".DinnerActivity" />
        <activity android:name=".LunchActivity" />
        <activity android:name=".BreakfastActivity" />
        <activity android:name=".NutritionActivity" />
        <activity android:name=".MainMenuActivity" />
        <activity android:name=".BmiTestActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>